values = []
increased = []
decreased = []
used =[]
#variables etc

while True:
line = file.readline()
if not line:
break
#while loop with break

x = line.strip()
values.append(x)

if len(values) == 4:
firstSum = int(values[0]) + int(values[1]) + int(values[2])
secondSum = int(values[1]) + int(values[2]) +  int(values[3])

if (firstSum > secondSum):
decreased.append(firstSum)
del values[0]

else:
increased.append(firstSum)
del values[0]

print(len(increased))
